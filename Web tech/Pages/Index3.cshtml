@page
@model Web_tech.Pages.Index3Model
@{
}
<!DOCTYPE html>
<html>
<head>
    <title>Widget Page</title>
    <style>
        /* CSS styles for the widgets */
        body {
            background-color: #2c3e50;
            color: #fff;
            font-family: 'Roboto', sans-serif;
        }

        .widget-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .widget {
            position: relative;
            width: 300px;
            height: 200px;
            background-color: #34495e;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.2);
            border-radius: 10px;
            padding: 20px;
            margin: 20px;
            cursor: pointer;
        }

            .widget:hover {
                transform: translateY(-5px);
                box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.4);
            }

            .widget:before {
                content: "";
                position: absolute;
                width: 100%;
                height: 10px;
                top: -10px;
                left: 0;
                background-color: #2c3e50;
                border-radius: 10px 10px 0 0;
            }

            .widget:after {
                content: "";
                position: absolute;
                width: 10px;
                height: 100%;
                top: 0;
                right: -10px;
                background-color: #2c3e50;
                border-radius: 0 10px 10px 0;
            }

        .widget-icon {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 50px;
        }

        .widget-label {
            position: absolute;
            bottom: 20px;
            left: 0;
            right: 0;
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        #widget1 .widget-icon {
            color: #e74c3c;
        }

        #widget2 .widget-icon {
            color: #2ecc71;
        }
    </style>
</head>
<body>
    <div class="widget-container">
        <a class="links_name" asp-area="" asp-page="/Index2">
        <div class="widget" id="widget1">
            <i class="widget-icon fas fa-search"></i>
            <div class="widget-label">Questionbank</div>
        </div>
        </a>
        <a class="links_name" asp-area="" asp-page="/CreateQuestionPaper">
        <div class="widget" id="widget2">
            <i class="widget-icon fas fa-envelope"></i>
            <div class="widget-label">CreateQuestion</div>
        </div>
        </a>
    </div>

    
</body>
</html>